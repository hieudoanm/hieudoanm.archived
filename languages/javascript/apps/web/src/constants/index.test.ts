import { APP_NAME, FIRST_YEAR } from '.';

describe('constants', () => {
  it('FIRST_YEAR', () => {
    expect(FIRST_YEAR).toEqual(1970);
  });
});

describe('constants', () => {
  it('APP_NAME', () => {
    expect(APP_NAME).toEqual('HIEU DOAN (hieudoanm)');
  });
});
