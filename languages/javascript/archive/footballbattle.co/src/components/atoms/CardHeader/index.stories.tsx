import CardHeader from '.';

export const CardHeaderComponent = () => <CardHeader>CardHeader</CardHeader>;

const Story = {
  title: 'atoms/CardHeader',
  component: CardHeader,
};

export default Story;
