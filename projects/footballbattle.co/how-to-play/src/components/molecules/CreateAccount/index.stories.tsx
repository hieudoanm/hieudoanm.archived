import CreateAccount from '.';

export const CreateAccountComponent = () => <CreateAccount />;

const Story = {
  title: 'molecules/CreateAccount',
  component: CreateAccount
};

export default Story;
