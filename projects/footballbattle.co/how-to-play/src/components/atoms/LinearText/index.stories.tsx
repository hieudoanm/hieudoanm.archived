import LinearText from '.';

export const LinearTextComponent = () => <LinearText>LinearText</LinearText>;

const Story = {
  title: 'atoms/LinearText',
  component: LinearText,
};

export default Story;
